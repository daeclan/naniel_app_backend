@causes.map do |cause|
  {
    id: cause.id, 
    name: cause.name,
    goal: cause.goal,
    description: cause.description,
    status: cause.status,
    progress: cause.progress,
    user_id: cause.user_id,
    user: cause.user
  }
end